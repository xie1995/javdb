# Background App

Service-worker bootstrapping and message routing will move here.

Expected responsibilities:

- Register runtime listeners.
- Route messages to platform services and feature entrypoints.
- Start scheduled background tasks.
