// src/features/newWorks/types.ts
// 兼容旧路径：新作品纯类型已提升到全局 types 层

export type {
  ActorSubscription,
  CollectionResult,
  ManualCheckResult,
  NewWorkRecord,
  NewWorksGlobalConfig,
  NewWorksSearchResult,
  NewWorksStats,
} from '../../types';
