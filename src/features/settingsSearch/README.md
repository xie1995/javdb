# Settings Search

Pilot feature for the new architecture.

Target behavior:

- Build a flat search index from dashboard setting labels and descriptions.
- Match user queries and aliases.
- Navigate to the target settings page.
- Scroll to the exact setting item.
- Highlight the target item with a short animation.

Public dashboard mounting should be exposed from this feature directory.
