# Shared

Pure cross-layer types, protocols, constants, and utility functions live here.

Shared code should be side-effect free and independent from DOM, Chrome APIs,
network access, and storage access.

Current top-level shared files remain in place during migration. New shared code
should prefer the subdirectories below.
